# Compressed LLM - dependencias
# Dependencias esenciales (ya instaladas en el entorno de desarrollo):
torch>=2.0
numpy>=1.24
datasets>=2.14
tokenizers>=0.15
pyyaml>=6.0
tqdm>=4.65

# Opcional / solo cuando se requiera (tokenizer compatible con HF):
transformers>=4.30

# Opcional: logging de experimentos
# wandb>=0.15